{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "theme": "./tools/theme/config.sh $THEME",
    "build": "npm run theme && nuxt build",
    "dev": "npm run theme && nuxt dev --open",
    "generate": "npm run theme && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.9.3",
    "vue": "^3.4.14",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.5.1",
    "pinia": "^2.1.7"
  }
}
